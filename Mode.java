package lab5;

public interface Mode{
	/**
	 * 
	 * Find integer value in array that is repeated more than once but two different values cannot be repeated  
	 * @param value integer of array that user input 
	 * @return value which is repeated more than once
	 */
	public  int getMode(int[] values);
	
}
